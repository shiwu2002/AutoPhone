# Excel 任务执行器使用说明

## 功能概述

`excel_task.py` 用于读取电脑上的 Excel/TXT 文件，让智能体根据文件内容在手机上执行任务。

**关键特性：**
- 文件在电脑上读取，不是在手机里
- 智能体不会误解为"在手机上查找文件"
- 支持 Excel (.xlsx, .xls) 和 TXT 格式

## 与批量任务的区别

| 功能 | `excel_task.py` | `main.py --batch` |
|------|-----------------|-------------------|
| 用途 | 让智能体读取文件内容后执行任务 | 批量执行多个独立问题 |
| 文件处理 | 读取内容后交给智能体 | 逐行读取，逐个执行 |
| 典型场景 | "打开微信，把文件里的问题发给张三" | "执行 100 个测试问题并导出结果" |

## 使用示例

### 基本用法

```bash
# 读取 Excel，打开微信发送给某人
python excel_task.py --file 工作簿 2.xlsx --task "打开微信，给峰峰峰回路转发送文档里的所有问题"
```

### 指定列名

```bash
# 如果 Excel 有多列，指定读取"问题"列
python excel_task.py --file questions.xlsx --column 问题 --task "打开微信，把所有问题发给峰峰峰回路转"
```

### 使用占位符

```bash
# 使用 {content} 占位符精确控制内容位置
python excel_task.py --file work.xlsx --task "打开微信，找到张三，然后发送以下内容：{content}"
```

### 先预览内容

```bash
# 先看看文件内容是否正确
python excel_task.py --file 工作簿 2.xlsx --preview
```

### 读取 TXT 文件

```bash
python excel_task.py --file questions.txt --task "在抖音搜索文档里的每个问题"
```

## Excel 文件格式

支持任意格式的 Excel 文件：

### 格式 1：单列问题

| 问题 |
|------|
| 什么是人工智能？ |
| 机器学习的方法有哪些？ |
| 深度学习应用场景有哪些？ |

### 格式 2：多列数据

| 序号 | 问题 | 答案 | 备注 |
|------|------|------|------|
| 1 | 什么是人工智能？| AI 是... | 基础题 |
| 2 | 机器学习的方法？| 有监督... | 进阶题 |

自动检测"问题"列，或用 `--column` 指定。

## Prompt 优化

### 问题：智能体误以为在手机上查文件

**之前的问题：**
```
任务：打开微信，发送工作簿 2.xlsx 里的所有问题
→ 智能体可能尝试在手机里查找文件
```

**现在的解决方案：**

`excel_task.py` 会自动添加明确的说明：

```
【重要说明】
我已经从电脑上的文件 "工作簿 2.xlsx" 中读取了所有内容。
注意：文件已经读取完成，不需要在手机上查找或打开任何文件。
请直接根据以下文件内容执行任务。

=== 文件内容开始 ===
1. 什么是人工智能？
2. 机器学习的方法有哪些？
3. 深度学习应用场景有哪些？
=== 文件内容结束 ===

用户任务：打开微信，给峰峰峰回路转发送文档里的所有问题
```

同时修改 system prompt：
```
【文件任务特别说明】
- 用户提到的文件（如 Excel、TXT）已经在电脑上读取完成
- 文件内容已经提供在用户任务中
- 不要在手机里查找或打开任何文件
- 直接根据提供的文件内容执行任务即可
```

## 典型场景

### 场景 1：发送消息

```bash
python excel_task.py --file 问题列表.xlsx --task "打开微信，给张三发送文件里的所有问题，一个一个发"
```

智能体会：
1. 读取 Excel 内容
2. 打开微信
3. 找到张三
4. 逐条发送问题

### 场景 2：搜索内容

```bash
python excel_task.py --file 搜索关键词.txt --task "在淘宝搜索文件里的每个商品，记录价格"
```

### 场景 3：批量操作

```bash
python excel_task.py --file 任务清单.xlsx --task "按照文件里的任务，一个一个执行"
```

## 安装依赖

```bash
pip install pandas openpyxl
```

## 常见问题

### Q1: 智能体还是在手机上找文件

确保使用的是 `excel_task.py` 而不是 `main.py --batch`。

`excel_task.py` 会自动添加明确说明，防止误解。

### Q2: 文件内容太多，智能体记不住

智能体有上下文限制，建议：
- Excel 文件不超过 50 行
- 或分批执行

### Q3: 找不到指定的列

默认会自动检测"问题"列，如果找不到，使用 `--column` 指定：

```bash
python excel_task.py --file data.xlsx --column Question
```

### Q4: 发送的消息格式不对

在任务中明确说明格式：

```bash
python excel_task.py --file q.xlsx --task "打开微信，把每个问题作为一条单独的消息发送"
```
